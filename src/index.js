import React from 'react';
import ReactDOM from 'react-dom/client';
import TaxSimpleApp from './taxsimple-app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TaxSimpleApp />
  </React.StrictMode>
);
